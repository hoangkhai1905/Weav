{
  "schemaVersion": "1.0",
  "nodes": [
    {
      "id": "manual-root",
      "type": "trigger.manual",
      "config": {}
    },
    {
      "id": "email",
      "type": "email.send",
      "config": {
        "to": "workflow-v1-dependency-test@example.test",
        "subject": "Workflow V1 dependency test",
        "body": "The unsupported email adapter must fail closed without sending."
      }
    }
  ],
  "edges": [
    { "id": "manual-email", "source": "manual-root", "target": "email" }
  ],
  "variables": {}
}
