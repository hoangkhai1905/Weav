# Notifications use HTTP unless mock mode is explicitly selected.
VITE_API_MODE=http
# VITE_API_GATEWAY_URL also remains supported and takes precedence if already set.
VITE_API_BASE_URL=http://localhost:3000
