FROM eclipse-temurin:25-jdk

WORKDIR /app

# Maven Wrapper cần dùng trong container
RUN apt-get update \
    && apt-get install -y --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

COPY .mvn/ .mvn/
COPY mvnw pom.xml ./

RUN chmod +x mvnw

COPY src/ src/

# Shared API contracts are a separate build context so root-level secrets stay outside the image build.
COPY --from=contracts . /packages/contracts/

CMD ["./mvnw", "spring-boot:run"]
