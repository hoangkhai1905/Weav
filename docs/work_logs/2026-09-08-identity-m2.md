# Nhật ký ngày `2026-09-08`

## 1. Metadata

| Trường | Giá trị |
| --- | --- |
| Ngày làm việc | `2026-09-08` |
| Múi giờ ghi log | `Asia/Saigon` |
| Dự án / repository | `Weav / Identity Service` |
| Nhánh / commit đầu ngày | `feat/identity-m2 / a21fbd0` |
| Người thực hiện | `Codex Identity M2 worker` |
| Người review / nhận bàn giao | `Identity coordinator` |
| Trạng thái cuối ngày | `Đang tiếp tục` |
| Phạm vi session | `Bounded M2 recovery/OTP fixes, focused verification, and handoff evidence` |
| Liên kết liên quan | `docs/superpowers/plans/2026-09-06-identity-remaining-endpoints.md` |

## 2. Tóm tắt điều hành

### Kết quả chính

- Fixed the known test compilation blocker by importing `MockMvcResultMatchers.header`.
- Isolated `OtpRecoveryHttpIntegrationTest` per test method so production `REGISTER_IP` limits remain unchanged; added bounded HTTP and future waits.
- Preserved Valkey account-slot authority while preventing the active account hash TTL from shortening the other OTP purpose; added an integration assertion.
- Added `no-store` to authentication rate-limit 429 responses and dependency 503 responses; added HMAC namespace delimiter validation.
- Added real HTTP acceptance coverage for stale credential challenge/grant rejection after a password change, concurrent reset/login ordering, reset-grant consumption across a database rollback, and common SMTP admission overload behavior for eligible/unknown/disabled/OAuth-only accounts.

### Tình trạng nhanh

| Hạng mục | Trạng thái | Ghi chú ngắn |
| --- | --- | --- |
| Build / compile | `PASS` | Full `test` and `package` with Maven 3.9.16, Java 25.0.4, UTC timezone, existing user Maven cache |
| Unit / integration test | `PASS` | Full Identity suite: 127/127; focused new acceptance suites: 8/8 (overlapping counts, not additive) |
| Migration / database | `PASS` | Disposable PostgreSQL applied V1, V2, V3 during HTTP tests |
| Health check | `Chưa kiểm tra` | No separate running-service health gate in this worker session |
| Review thay đổi | `Cảnh báo cần review` | `git diff --check` clean; dedicated GitNexus detect_changes returned critical for the broad M2 WIP |
| Commit / PR | `Chưa tạo` | Worker must not commit or merge; coordinator owns milestone decision |

## 3. Mục tiêu và phạm vi

### Mục tiêu đầu session

1. Clear the known `testCompile` blocker and rerun the requested M2 suites.
2. Make recovery tests deterministic without raising production limits.
3. Review bounded timeouts, Valkey atomic state, sensitive output, and error headers.

### Trong phạm vi

- Isolated worktree `T:\Weav\tmp\identity-m2-worktree`, branch `feat/identity-m2`.
- Identity M2 OTP, Valkey challenge/grant, SMTP dispatcher tests, HTTP recovery/concurrency tests, and shared error handlers.
- Disposable Docker-backed PostgreSQL, Valkey, and Mailpit only.

### Ngoài phạm vi / chủ động chưa làm

- No commit, merge, push, Neon writes, secret reads, `.env` reads, or production rate-limit changes.
- No claim that the entire M2 milestone is complete; coordinator still owns full-service regression and final runtime review.

### Tiêu chí hoàn thành

- [x] Known test compilation error fixed.
- [x] Focused Valkey/HMAC/error-handler tests pass.
- [x] HTTP recovery and concurrent redemption tests pass with bounded waits.
- [x] Final GitNexus `detect_changes(scope=all)` was executed on the dedicated worktree index.
- [x] Full Identity service regression and package build completed: 127/127 tests pass and jar repackaged.
- [ ] Coordinator acceptance/review of the critical graph result and milestone commit remain pending.

## 4. Bối cảnh và giả định

- **Bối cảnh hệ thống:** M1 was committed at `a21fbd0`; M2 WIP already contained V3 nullable email verification state, Valkey OTP/grant state, SMTP dispatch, use cases, controllers, contracts, and tests.
- **Giả định đã dùng:** The local Docker daemon and disposable containers are valid test dependencies; production limits must stay as configured.
- **Ràng buộc:** Maven wrapper is broken; use the prescribed Maven 3.9.16 binary with Java 25.0.4 and `JAVA_TOOL_OPTIONS=-Duser.timezone=UTC`. Do not expose secrets or test message payloads.
- **Nguồn sự thật:** Current source, test output, `AGENTS.md`, and the M2 implementation plan; stale prior documentation is not treated as runtime evidence.

## 5. Nhật ký theo session / thời gian

### Session `1` - `2026-09-08`

| Thời điểm | Việc đã thực hiện | Kết quả / bằng chứng | Trạng thái |
| --- | --- | --- | --- |
| `10:30` | Read `AGENTS.md`, M2 plan, planning work log, current status/diff, Java/security/GitNexus guidance | Confirmed isolated worktree and M2 WIP scope | Xong |
| `10:36` | Ran Maven `test-compile` with prescribed JDK/timezone and user cache | `BUILD SUCCESS`; 119 main and 44 test sources compiled | Xong |
| `10:40` | Ran `OtpChallengeStoreIntegrationTest`, `OtpRecoveryHttpIntegrationTest`, `PasswordRecoveryConcurrencyIntegrationTest` | Valkey 7/7 pass; HTTP suite had one reproducible 429 from shared register limiter; concurrency 1/1 pass | Xong |
| `10:42` | Added static `header` import, per-method Spring context isolation, HTTP/future timeouts | Production register limit unchanged; test fixture no longer shares limiter state | Xong |
| `10:46` | Added active-hash TTL preservation, HMAC namespace delimiter guard, and no-store error headers | Added focused tests and assertions | Xong |
| `10:48` | Ran HMAC, Valkey, and exception-handler tests | `BUILD SUCCESS`, 16 tests, 0 failures/errors/skips | Xong |
| `10:49` | Reran HTTP recovery and concurrency suites | `BUILD SUCCESS`, 4 tests, 0 failures/errors/skips; V1/V2/V3 disposable migrations applied | Xong |
| `17:58` | Ran GitNexus `detect_changes(scope=all)` against the isolated worktree | `critical`; 108 changed symbols, 62 affected processes, 25 changed files; coordinator review/reindex required | Cảnh báo |

### Session `2` - `2026-09-08` - M2 acceptance closure

| Thời điểm | Việc đã thực hiện | Kết quả / bằng chứng | Trạng thái |
| `18:05` | Added real HTTP stale-credential coverage | `OtpRecoveryHttpIntegrationTest`: password change invalidates an old email challenge and stale reset grant; fresh password works and old password fails | Xong |
| `18:10` | Added real concurrent reset/login ordering coverage | `PasswordRecoveryConcurrencyIntegrationTest`: reset wins consistently; any concurrent login session is rejected after reset; old password fails and new password succeeds | Xong |
| `18:15` | Added real database rollback/grant-consumption coverage | PostgreSQL trigger forces reset `500`; same reset grant is rejected after rollback, old password remains valid, new password is not applied | Xong |
| `18:20` | Added real HTTP bounded mail-admission coverage | `OtpMailAdmissionHttpIntegrationTest`: eligible/unknown/disabled/OAuth-only requests all return bounded `503 DEPENDENCY_UNAVAILABLE` with `Cache-Control: no-store`; pre-existing core session remains `200` | Xong |
| `18:25` | Reran focused acceptance suites | `BUILD SUCCESS`; `Tests run: 8, Failures: 0, Errors: 0, Skipped: 0` | Xong |
| `18:35` | Ran complete Identity regression | `BUILD SUCCESS`; `Tests run: 127, Failures: 0, Errors: 0, Skipped: 0` | Xong |
| `18:39` | Ran complete Identity package build | `BUILD SUCCESS`; 127 tests passed again; Spring Boot jar repackaged at `services/identity-service/target/identity-service-0.0.1-SNAPSHOT.jar` | Xong |
| `18:45` | Refreshed dedicated GitNexus index and ran final `detect_changes(scope=all)` | Index `Weav-identity-m2` at `a21fbd0`; detect result `critical`, 25 files/118 symbols/60 processes. Analyzer warned FTS unavailable and flow truncation (50 entry candidates dropped, 359 callees skipped); no clean completeness claim | Cảnh báo |

### Diễn giải quan trọng

The original HTTP failure was not a production defect: three test methods shared one Spring context and the real `REGISTER_IP` limiter (5/min), so the sixth registration returned 429. `@DirtiesContext(BEFORE_EACH_TEST_METHOD)` gives each method a fresh in-memory limiter and disposable database context without weakening production policy. HTTP requests now have a 10-second timeout; concurrent futures use a 15-second bound.

The Valkey active account hash is shared by purpose pointers. Reset grant verification now extends the hash only when needed to cover the remaining email challenge TTL; challenge issuance applies the same max-TTL rule for a retained purpose/grant. The test checks that active TTL is not below the other challenge TTL.

## 6. Quyết định kỹ thuật

| Quyết định | Lý do / bằng chứng | Phương án đã cân nhắc | Hệ quả và việc theo dõi |
| --- | --- | --- | --- |
| Isolate HTTP recovery tests by context | Shared limiter caused a real 429 after six registrations | Raise `REGISTER_IP` only in tests; rejected because it masks production behavior | More startup time, but deterministic disposable tests |
| Preserve active Valkey TTL by maximum retained-purpose TTL | A reset grant TTL must not expire an active email challenge early | Separate active keys; deferred because it would broaden the WIP storage design | Keep account-slot generation authoritative; retain focused TTL test |
| Add `no-store` for auth 429 and dependency 503 | Error responses can carry security-sensitive request context and must not be cached | Only protect OTP controller responses; rejected because filter/advice paths also emit 429/503 | Shared response behavior is safer; rerun core error tests |

## 7. Thay đổi đã thực hiện

### 7.1. Code và hành vi

- `ValkeyOtpChallengeStore`: active account hash expiry now respects retained challenge/grant TTLs; per-account generation pointers remain authoritative.
- `HmacKeyedFingerprint`: rejects a NUL in the namespace delimiter position while allowing intentional NUL-separated composite values.
- `GlobalExceptionHandler`, `AuthRateLimitExceptionHandler`, `AuthRateLimitFilter`: 429/503 responses use `Cache-Control: no-store`; rate-limit responses retain `Retry-After`.
- `OtpRecoveryHttpIntegrationTest`, `PasswordRecoveryConcurrencyIntegrationTest`: deterministic context/fixture behavior and bounded HTTP/future waits.
- `OtpRecoveryHttpIntegrationTest`: real stale email-challenge and stale password-reset-grant rejection after a password change.
- `PasswordRecoveryConcurrencyIntegrationTest`: real concurrent reset/login ordering and PostgreSQL rollback after one-time reset grant consumption.
- `OtpMailAdmissionHttpIntegrationTest`: real HTTP/Valkey/PostgreSQL common bounded-admission and dependency-failure behavior across eligible, unknown, disabled, and OAuth-only accounts.
- `GlobalExceptionHandlerTest`, `HmacKeyedFingerprintTest`, `OtpChallengeStoreIntegrationTest`: regression assertions for the above behavior.
- `AuthRateLimitFilterTest`: regression assertion for filter-level no-store responses.

### 7.2. Dữ liệu, schema và migration

- Disposable Identity PostgreSQL tests applied existing WIP V1/V2 plus additive `V3__add_user_email_verified_at.sql`.
- No persistent or Neon data was changed by this worker.

### 7.3. Cấu hình, hạ tầng và dependency

- Tests used disposable `postgres:18-alpine`, `valkey/valkey:8-alpine`, and `axllent/mailpit:v1.21.8` containers.
- No production limit, secret, `.env`, or external provider configuration was changed.

### 7.4. API, bảo mật và quan sát hệ thống

- OTP/recovery routes keep opaque 43-character challenge/reset IDs and no-store success responses from existing WIP.
- Rate-limit responses expose only generic messages, `Retry-After`, and no-store headers; no OTP/grant/password payload is logged or returned.

## 8. Danh sách file ảnh hưởng

| Loại | Đường dẫn | Thay đổi chính | Lưu ý cho người tiếp nhận |
| --- | --- | --- | --- |
| `Sửa` | `services/identity-service/src/main/java/com/weav/identity/infrastructure/authstate/ValkeyOtpChallengeStore.java` | Preserve active hash TTL across OTP purposes | Review Lua atomicity and index cleanup |
| `Sửa` | `services/identity-service/src/main/java/com/weav/identity/infrastructure/authstate/HmacKeyedFingerprint.java` | Guard namespace delimiter | Composite OTP code values intentionally remain NUL-separated |
| `Sửa` | `services/identity-service/src/main/java/com/weav/identity/infrastructure/web/GlobalExceptionHandler.java` | Add no-store for 429/503 response builder paths | Recheck all shared error callers |
| `Sửa` | `services/identity-service/src/main/java/com/weav/identity/infrastructure/security/AuthRateLimitExceptionHandler.java` | Add no-store to auth limiter advice | Existing rate limit remains unchanged |
| `Sửa` | `services/identity-service/src/main/java/com/weav/identity/infrastructure/security/AuthRateLimitFilter.java` | Add no-store to filter 429 path | Applies before controller invocation |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/presentation/http/OtpRecoveryHttpIntegrationTest.java` | Per-method context and HTTP timeout | Uses disposable context; no production override |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/presentation/http/PasswordRecoveryConcurrencyIntegrationTest.java` | HTTP/future timeouts | Race remains real concurrent HTTP |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/presentation/http/OtpRecoveryHttpIntegrationTest.java` | Stale challenge/grant rejection after password change | Real HTTP, PostgreSQL, Valkey, and Mailpit path |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/presentation/http/PasswordRecoveryConcurrencyIntegrationTest.java` | Concurrent reset/login and DB rollback after grant consumption | PostgreSQL trigger is created/dropped inside disposable test DB |
| `Thêm` | `services/identity-service/src/test/java/com/weav/identity/presentation/http/OtpMailAdmissionHttpIntegrationTest.java` | Common bounded mail admission and dependency 503 behavior | Uses a blocking test sender; does not claim live SMTP latency delivery |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/infrastructure/authstate/OtpChallengeStoreIntegrationTest.java` | Active TTL regression | Uses Valkey container and two clients |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/infrastructure/authstate/HmacKeyedFingerprintTest.java` | Namespace delimiter regression | No secret values in assertions |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/infrastructure/security/AuthRateLimitFilterTest.java` | Filter no-store regression | Covers pre-controller 429 path |
| `Sửa` | `services/identity-service/src/test/java/com/weav/identity/infrastructure/web/GlobalExceptionHandlerTest.java` | Compile import and cache-header assertions | Known blocker cleared |
| `Thêm` | `docs/work_logs/2026-09-08-identity-m2.md` | Session evidence and pending gates | Update before final handoff |

Other M2 WIP files were pre-existing in this isolated worktree and remain under coordinator review; no pre-existing file was deleted.

## 9. Kiểm tra và bằng chứng

| Hạng mục | Lệnh / thao tác tái lập | Kết quả thực tế | Phạm vi và giới hạn |
| --- | --- | --- | --- |
| Compile/build | Maven 3.9.16 `-DskipTests test-compile` with `JAVA_HOME=jdk-25.0.4`, UTC, user cache | `PASS` | Identity module compile/testCompile only |
| Test | `-Dtest=OtpChallengeStoreIntegrationTest,HmacKeyedFingerprintTest,GlobalExceptionHandlerTest test` | `PASS`, 16 tests | Valkey container, unit tests, MVC advice tests |
| Test | `-Dtest=OtpRecoveryHttpIntegrationTest,PasswordRecoveryConcurrencyIntegrationTest,OtpMailAdmissionHttpIntegrationTest test` | `PASS`, 8 tests | Real HTTP + PostgreSQL + Valkey + Mailpit for recovery; blocking sender for bounded admission; test log masks payloads |
| Test | `-Dtest=AuthRateLimiterTest,AuthRateLimitFilterTest,GlobalExceptionHandlerTest,HmacKeyedFingerprintTest,OtpPropertiesTest,BoundedAuthMailDispatcherTest,SmtpAuthMailSenderTest,RequestOtpUseCaseTest,ResetPasswordUseCaseTest,VerifyOtpUseCaseTest,SensitivePayloadToStringTest test` | `PASS`, 41 tests | M2 unit/error paths |
| Test | `-Dtest=SmtpAuthMailSenderIntegrationTest test` | `PASS`, 1 test | Disposable Mailpit SMTP delivery |
| Full regression | Maven `-B -Dstyle.color=never -Dmaven.repo.local=C:/Users/nhoan/.m2/repository test` | `PASS`, 127 tests, 0 failures/errors/skips | Complete Identity test suite; includes the focused suites, so counts must not be summed |
| Package | Maven `-B -Dstyle.color=never -Dmaven.repo.local=C:/Users/nhoan/.m2/repository package` | `PASS`, 127 tests again; jar repackaged | `services/identity-service/target/identity-service-0.0.1-SNAPSHOT.jar`; package reruns the same suite |
| DB migration | Same HTTP integration command | `PASS`, disposable schema reached V3 | No Neon or persistent DB write |
| Static/diff check | `git diff --check` | `PASS` | Existing WIP diff still requires coordinator review |
| GitNexus impact/change review | Dedicated index `Weav-identity-m2`; upstream impact for `RequestOtpUseCase`, `VerifyOtpUseCase`, `ResetPasswordUseCase`, `BoundedAuthMailDispatcher`, and the two integration-test entry points; then `detect_changes(scope=all)` | Impacts: use cases `MEDIUM`, dispatcher `LOW`, test entry points `UNKNOWN` corroborated as test-only. Final detect: `critical`, 25 files/118 symbols/60 processes. Index analysis warned FTS unavailable plus flow truncation (50 entry candidates dropped, 359 callees skipped); no clean completeness claim | Manual source inspection completed; coordinator must review broad M2 blast radius before commit |

### Điều chưa được kiểm tra

- Running-service health was not separately checked in this worker; tests exercised disposable HTTP application contexts.
- The existing `SmtpAuthMailSenderIntegrationTest` is included in the full 127-test regression and package rerun (its dedicated Surefire report is 1/1 pass); the new overload test uses a blocking `AuthMailSender` to prove common admission and bounded failure, not live SMTP latency delivery.
- GitNexus graph completeness remains limited by the analyzer's FTS/flow warnings; `critical` is not treated as a clean or complete result.
- No final commit or push was created.

## 10. Sự cố, rủi ro và blocker

| Mức độ | Vấn đề | Nguyên nhân / dấu hiệu | Cách xử lý hiện tại | Chủ sở hữu / bước tiếp theo |
| --- | --- | --- | --- | --- |
| `Cao` | GitNexus graph review reports critical risk and incomplete flow coverage | Dedicated index at `a21fbd0` reports 25 files, 118 symbols, and 60 processes; analyzer warned FTS unavailable and dropped 50 entry candidates / skipped 359 callees | No further source edits; result and completeness warnings are explicit | Coordinator reviews affected processes and decides whether to reindex/inspect before commit |
| `Thấp` | Maven wrapper cannot be used | Wrapper target entry is broken and default `C:\.m2` is not writable | Used prescribed Maven 3.9.16 binary with user cache | Keep wrapper issue out of source changes |
| `Trung bình` | Context-isolated test suite is slower | Each method starts a fresh Spring/PostgreSQL context | Accepted for deterministic real-limit testing | Coordinator may optimize fixture seeding later without limit changes |

### Lỗi có thể tái lập

```text
Before isolation: OtpRecoveryHttpIntegrationTest.enforcesEmailPresenceRulesAndAuthenticatedOwnership
failed at register() with expected 201 but received 429 because tests shared the production REGISTER_IP 5/min limiter.
```

## 11. Trạng thái bàn giao

### Có thể tiếp tục ngay

1. Review this log and the focused diff in `T:\Weav\tmp\identity-m2-worktree`.
2. Review the full 127-test and package evidence; do not add focused/full/package counts because they overlap.
3. Review the critical GitNexus result and analyzer completeness warnings before any milestone commit.

### Cần quyết định / quyền truy cập từ người khác

- Coordinator must decide final M2 scope, commit grouping, and whether remaining provider/runtime gates are complete; this worker did not commit or push.

### Hướng dẫn cho AI agent tiếp theo

- Preserve all pre-existing WIP and the accidental `compose.dev.yml` newline state; do not delete files.
- Read the M2 plan Tasks 4-5, this log, and `git status` before editing.
- Treat passing focused tests as evidence for those paths only, not as a complete M2 claim.
- Do not read or add secrets; keep test payload output redacted.

## 12. Tham chiếu

- `AGENTS.md`
- `docs/superpowers/plans/2026-09-06-identity-remaining-endpoints.md`
- `docs/work_logs/2026-09-06-identity-remaining-endpoints-planning.md`
- `docs/work_logs/log_template.md`

## 13. Kết thúc session

| Trường | Giá trị |
| --- | --- |
| Thời điểm dừng | `2026-09-08 18:45 Asia/Saigon` |
| Trạng thái worktree | `Có M2 WIP và bounded fixes chưa commit` |
| Commit/PR đã tạo | `Chưa tạo` |
| Người cập nhật log | `Codex Identity M2 worker` |
| Cần đọc trước khi tiếp tục | `M2 plan Tasks 4-5; this log; focused test reports; git status` |

---

## Checklist trước khi đóng log

- [x] Tóm tắt nói rõ kết quả và phần chưa hoàn thành.
- [x] Mọi quyết định ảnh hưởng thiết kế đều có lý do.
- [x] File thay đổi và migration/dependency quan trọng đã được nêu.
- [x] Có lệnh hoặc thao tác tái lập cho các kiểm tra đã tuyên bố.
- [x] Rủi ro, blocker và next step có chủ sở hữu hoặc hành động rõ ràng.
- [x] Không có secret, token, connection string nhạy cảm hoặc PII không cần thiết.
- [x] Trạng thái commit/PR và worktree là chính xác tại thời điểm ghi.

---

## 14. Final M2 socket/SMTP failure gate (2026-09-08 19:17 Asia/Saigon)

### Kết quả bounded

- Added only `services/identity-service/src/test/java/com/weav/identity/presentation/http/SmtpFailureHttpIntegrationTest.java`; no production source was changed in this gate.
- The test uses a deterministic loopback `ServerSocket` SMTP protocol peer, not Mailpit, a real inbox, or an external provider. It covers SMTP rejection after `DATA` and a read-timeout/stall after `DATA`.
- Each test runs the real Spring HTTP application with disposable PostgreSQL and Valkey containers and the production SMTP adapter/async dispatcher. HTTP admission is asserted below one second while SMTP failure is observed through latches; the eligible challenge is invalidated, a different accepted challenge remains valid, and pre-existing core login/`/users/me` remain available.
- The timeout case submits eligible, unknown, disabled, and OAuth-only categories; all receive `202` within the bound, only the eligible account opens an SMTP connection, and its challenge is invalidated after the timeout.

### Verification evidence (counts are non-additive)

| Gate | Command/evidence | Result |
| --- | --- | --- |
| Focused socket gate | Maven 3.9.16, `-Dtest=SmtpFailureHttpIntegrationTest test`; Surefire `services/identity-service/target/surefire-reports/com.weav.identity.presentation.http.SmtpFailureHttpIntegrationTest.txt` | `Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS` |
| Full Identity regression | Maven `test` with Java 25 and UTC | `Tests run: 129, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS` at `2026-09-08T12:15:03Z` |
| Package gate (session `48601`) | Maven `package`, same Java/Maven/cache settings | `Tests run: 129, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS` at `2026-09-08T12:17:09Z`; repackaged `services/identity-service/target/identity-service-0.0.1-SNAPSHOT.jar` |
| Diff hygiene | `git diff --check` after this log update | `PASS`; only Git's existing LF/CRLF normalization warnings for two pre-existing properties files |

The previous full/package baseline was 127 tests. The current 129 count includes the two new socket tests; focused/full/package results must not be summed.

### GitNexus review and impact evidence

- Rebuilt the dedicated `Weav-identity-m2` index at commit `a21fbd0`; status reported current/up-to-date with 4,686 nodes, 9,765 edges, and 236 flows. The repository CLI wrapper still selects an unavailable runner, so the installed GitNexus CLI artifact was used directly; no application source was changed to satisfy the graph.
- Upstream impact before editing existing symbols: `SmtpAuthMailSender` `MEDIUM` (5 direct callers; auth mail/config flow), `RequestOtpUseCase` `MEDIUM` (7 direct callers/controllers/config/tests), and `BoundedAuthMailDispatcher` `LOW` (3 direct test callers). The new test entry point was `UNKNOWN` and was corroborated as test-only by its source path and content; it was not treated as unused.
- `detect-changes --scope all` returned `critical`: 25 files, 118 symbols, and 60 processes. Analyzer warnings remain material: FTS unavailable, 50 entry candidates dropped, and 359 callees skipped. Therefore this is not a clean completeness claim; the coordinator must review critical callers/processes and the truncated graph before any commit.

The visible critical-flow mapping is bounded as follows; the analyzer only printed 10 of the 60 affected flows, so the remaining 50 are intentionally not inferred:

| Visible process/flow | Relevant evidence in this gate |
| --- | --- |
| `authMailSender` (`RejectLineBreaks`, `RequirePositive`, `GetHost`, `GetPort`, `GetUsername`) | Existing SMTP sender unit/integration coverage plus `SmtpFailureHttpIntegrationTest` exercising the configured adapter over loopback rejection/timeout |
| `doFilterInternal` (`Decision`, `Window`, `Count`) | Existing `AuthRateLimitFilterTest` and the full 129-test regression; no filter source was changed in this gate |
| `respond` (`ApiErrorResponse`, `ErrorBody`) | Existing `GlobalExceptionHandlerTest` and real HTTP assertions in the recovery/socket suites |
| Other 50 affected flows | Not completely mapped because GitNexus reported truncation (50 entry candidates dropped / 359 callees skipped); coordinator review required |

### Remaining gaps / decisions

- The new failure/timeout proof is provider-free by design; it does not claim external-provider delivery or production running-service health. Mailpit success remains covered by the existing integration test.
- The asynchronous in-memory executor/queue remains process-local and non-durable; durability/backpressure policy is outside this bounded gate.
- No commit, merge, or push was created. Coordinator must decide whether the remaining provider/runtime and GitNexus completeness gates are sufficient for the M2 milestone.

---

## 15. Causal SMTP admission gate revision (2026-09-08 19:36 Asia/Saigon)

### Bounded change

- Revised only `services/identity-service/src/test/java/com/weav/identity/presentation/http/SmtpFailureHttpIntegrationTest.java`; no production source, migration, configuration file, secret, or provider was changed.
- The loopback SMTP peer now holds the connection after receiving the complete `DATA` body and before sending the final `550` rejection or timing out. The test starts the real HTTP request asynchronously, waits for the SMTP server's pending-session latch, asserts the socket is still live and no SMTP outcome was sent, then requires HTTP `202` within one second before releasing rejection or allowing the configured read timeout to occur.
- Rejection still invalidates only its challenge while another accepted challenge remains valid. Timeout covers eligible, unknown, disabled, and OAuth-only categories; only the eligible account opens SMTP, and its challenge is invalidated after the actual read-timeout failure. Cleanup releases pending latches, closes sockets, and waits at most five seconds for server sessions to quiesce.

### Verification

| Gate | Command/evidence | Result |
| --- | --- | --- |
| Causal focused gate | Maven 3.9.16, Java 25.0.4, UTC, `-Dtest=SmtpFailureHttpIntegrationTest test`; Testcontainers PostgreSQL 18 + Valkey 8; Surefire report | `Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS` at `2026-09-08T12:35:56Z` |
| Full Identity regression (session `71985`) | Maven 3.9.16, Java 25.0.4, UTC, full `test`; Testcontainers PostgreSQL 18 + Valkey 8 | `Tests run: 129, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS` at `2026-09-08T12:38:46Z` |
| Package gate (session `29107`) | Maven 3.9.16, Java 25.0.4, UTC, `package`; same Testcontainers/cache settings | `Tests run: 129, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS` at `2026-09-08T12:40:49Z`; repackaged `services/identity-service/target/identity-service-0.0.1-SNAPSHOT.jar` |
| Diff hygiene | `git diff --check` after causal test edit | PASS; only pre-existing LF/CRLF normalization warnings in two properties files |

### Proof boundary and remaining limitations

- The latch ordering would fail if SMTP were synchronous: the HTTP future is awaited while the server deliberately withholds its SMTP final response. The elapsed assertion alone is no longer the proof.
- The timeout read setting is `2s`, safely longer than the one-second HTTP admission bound; the test separately observes the server-side failed outcome after at least one second and waits for socket closure. This remains a deterministic provider-free loopback test, not external-provider delivery or running-service health.
- GitNexus remains bound to `Weav-identity-m2` index `a21fbd0`; the existing `critical` 25-file/118-symbol/60-process result and FTS/flow completeness warnings remain unresolved graph blind spots, not demonstrated source defects. No commit, merge, or push was created.

---

## 16. Final bounded production audit (2026-09-08 19:52 Asia/Saigon)

### Audit verdict

- Review-only pass over the existing OTP request/verify/reset, Valkey state, SMTP adapter/dispatcher, security admission, and error-response paths. No production source was edited, no tests were run, and no commit or push was created in this audit.
- No concrete production blocker was identified in the bounded source review. The account-slot Lua authority, purpose/credential binding, at-most-once reset-grant consumption, bounded asynchronous SMTP admission, failure invalidation path, no-store headers, and sanitized dependency errors are consistent with the M2 plan.
- One residual availability edge is recorded for follow-up, but is not classified as an authentication bypass: if Valkey is unavailable while asynchronous SMTP failure cleanup runs, the invalidation callback is best-effort and the challenge remains protected by its existing TTL. Evidence: `services/identity-service/src/main/java/com/weav/identity/infrastructure/mail/BoundedAuthMailDispatcher.java:131-137`.

### Bounded source-to-test mapping

| Production flow | Existing acceptance/unit evidence reviewed |
| --- | --- |
| OTP request, generic recovery receipt, purpose/identity checks | `RequestOtpUseCaseTest`, `OtpRecoveryHttpIntegrationTest`, `OtpMailAdmissionHttpIntegrationTest` |
| OTP verification, purpose binding, stale credential/session rejection | `VerifyOtpUseCaseTest`, `OtpRecoveryHttpIntegrationTest`, `PasswordRecoveryConcurrencyIntegrationTest` |
| Reset-grant consume, DB rollback, session revocation and login ordering | `ResetPasswordUseCaseTest`, `PasswordRecoveryConcurrencyIntegrationTest`, `OtpRecoveryHttpIntegrationTest`, `PasswordChangeConcurrencyIntegrationTest` |
| Valkey atomic issue/verify/consume/invalidate, TTL retention and keyed fingerprints | `OtpChallengeStoreIntegrationTest`, `HmacKeyedFingerprintTest`, `OtpPropertiesTest` |
| SMTP configuration/sender, bounded queue, reject/timeout cleanup and HTTP admission | `SmtpAuthMailSenderTest`, `SmtpAuthMailSenderIntegrationTest`, `BoundedAuthMailDispatcherTest`, `SmtpFailureHttpIntegrationTest`, `OtpMailAdmissionHttpIntegrationTest` |
| Sanitized dependency/rate-limit responses and cache policy | `GlobalExceptionHandlerTest`, `AuthRateLimitFilterTest`, `OtpMailAdmissionHttpIntegrationTest`, `SmtpFailureHttpIntegrationTest` |

This mapping is bounded to source and test names; it does not claim that dynamic call links are fully resolved by the analyzer.

### Corrected GitNexus evidence for precommit review

- The prior `detect_changes --scope all` note of `25 files / 118 symbols / 60 processes` was incomplete because it excluded 49 untracked new files from the worktree. It must not be used as the full M2 result.
- The correctly bound `Weav-identity-m2` worktree result covers all staged M2 files: `74 files / 754 symbols / 123 processes`, risk `CRITICAL`; all result arrays were returned and the response had no `partial` or `truncated` flags.
- Analyzer limitations remain separate from response truncation: FTS was unavailable, 50 entry candidates were dropped, and 359 callees were skipped. These are residual graph-coverage limits, so the result is not a claim of full graph completeness even though the returned arrays were complete for that run.

### Remaining acceptance gates

- No running-service health check, Neon smoke, or external SMTP-provider/inbox smoke was performed in this audit. Existing disposable PostgreSQL/Valkey/mail-catcher and loopback SMTP evidence remains valid within its stated boundary; provider/runtime acceptance is still a coordinator decision.
- The M2 worktree remains uncommitted and unpushed. Coordinator must review the corrected critical graph result and decide the milestone commit gate.
