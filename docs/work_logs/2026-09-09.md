# Nhật ký làm việc - 2026-09-09

## Metadata

- Repository: Weav
- Branch: `feature/ocr-service-production`
- Scope: cập nhật tài liệu thông số model và benchmark OCR theo implementation/evidence hiện tại.
- Status: Hoàn thành phần cập nhật tài liệu; chưa commit/push thay đổi mới.

## Thay đổi và quyết định

- Cập nhật `docs/development/OCR_BENCHMARK.md` để phân biệt local smoke evidence với production acceptance gate.
- Ghi đúng model mapping hiện tại: `PP-OCRv5_mobile_det`, `PP-OCRv6_medium_rec` cho `vi`/`vi+en`, và `en_PP-OCRv5_mobile_rec` cho `en`.
- Bổ sung thông số Paddle/PaddleX/OpenCV, PDF DPI, preprocessing, adaptive fallback, input/output caps và kết quả CER/WER hiện tại.
- Ghi dated correction cho các đoạn cũ mô tả `latin` hoặc model provisioning là pending.
- Không đưa model weights, corpus ảnh, secret hoặc nội dung raw OCR vào log.

## Bằng chứng

- `git diff --check -- docs/development/OCR_BENCHMARK.md`: PASS.
- Nguồn đối chiếu: `services/ocr-service/config/model-manifest.json`, `services/ocr-service/pyproject.toml`, implementation OCR và benchmark real OCR ngày 2026-09-08.

## Còn pending

- Bổ sung parameter count, vocabulary size, training corpus và input tensor shape từ model cards nếu cần cho luận văn; hiện không suy đoán các giá trị này.
- Held-out corpus >= 60 trang, production CER <= 5%, table F1/diacritic gate, warmed p95/RSS/load và authenticated Workflow/Neon E2E vẫn chưa đóng.

## Bàn giao

- File cần review: `docs/development/OCR_BENCHMARK.md`.
- Worktree có thay đổi cũ ngoài phạm vi; không staging/commit các phần đó.
- Không có `.env` nào được đọc, sửa hoặc đưa vào log.

## Model directory inventory update

- Documented the model folders observed in the host PaddleX cache and separated active text-OCR artifacts from alternative/legacy OCR models and PP-StructureV3 table/layout/orientation artifacts.
- Verified against `services/ocr-service/config/model-manifest.json`, `scripts/setup-ocr-model.ps1`, `compose.ocr-models.dev.yml`, and the Paddle OCR/table adapters.
- No code, model weights, `.env`, or secrets were changed.
- `git diff --check -- docs/development/OCR_BENCHMARK.md`: PASS after the inventory update.
